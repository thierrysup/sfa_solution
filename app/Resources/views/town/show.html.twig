{% extends 'base.html.twig' %}

{% block body %}
    <h1>Town</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ town.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ town.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ town.description }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{% if town.status %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('town_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('town_edit', { 'id': town.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
